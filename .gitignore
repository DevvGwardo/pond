node_modules/
dist/
# Generated image/asset output (e.g. output/imagegen). If a file under here is a
# real brand asset that should be versioned, commit it explicitly with `git add -f`.
output/
*.js
*.d.ts
*.d.ts.map
*.js.map
!.gitkeep
!bin/pond.js
!bin/pondsh.js
!bin/pond-mcp.js
!eslint.config.js

# Secrets / local state — never commit. The deploy runbook (deploy/README.md,
# deploy/upgrade.sh, docs/operations.md) assumes these are ignored.
.env
.env.pond.server
.pond/
deploy/cloudflared/config.yml
deploy/cloudflared/credentials.json
*.db
*.sqlite
*.sqlite3
.claude/

# Local scratch projects that live inside the repo dir but are NOT part of pond:
# a standalone pond capsule app (has its own .git) and a Remotion video project
# (~570 MB of node_modules + rendered .mp4s). Keep them out of pond's history.
professional-modern-landing-page-project/
video/
