// Client runtime — hooks and components for pond capsules
//
// In the current alpha, the client runtime is bundled directly
// into the user's client code. This file provides the type
// contracts and will eventually hold the actual hook implementations.

// Re-export for consumption
export {};

// These will be available at runtime when the client bundle loads.
// For now, they're provided by the dev server's HTML shell.
